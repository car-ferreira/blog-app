<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-2">
      <div class="card mt-4">
        <div class="card-body">
          <h2 class="card-title">{{postagem.titulo}}</h2>
          <hr>
          <p class="card-text">{{postagem.conteudo}}</p>
          <p class="card-text"><small class="text-muted">Data de publicação: {{postagem.data}}</small></p>
          <a href="/" class="btn btn-primary">Voltar</a>
        </div>
      </div>
    </div>
  </div>
</div>
